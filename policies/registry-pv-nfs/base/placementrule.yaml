apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: placement-policy-registry-pv-nfs
spec:
  clusterSelector:
    matchExpressions:
      - key: environment
        operator: In
        values:
          - lab
